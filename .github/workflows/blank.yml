name: Fourth Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print OK
        run: echo "OK"
      - name: Print GO version
        run: go version
